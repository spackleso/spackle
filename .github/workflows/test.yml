name: CI
on: push
jobs:
  test:
    runs-on: ubuntu-latest

    services:
      supabase:
        image: supabase

    steps:
      - uses: actions/checkout@v2
      - uses: supabase/setup-cli@v1
      - name: Install modules
        run: yarn
      - name: Run tests
        run: yarn test